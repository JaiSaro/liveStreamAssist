import React from 'React';
import {ScrollView} from 'react-native';

function LiveStreamDetailPage({navigation}: any): React.JSX.Element {
  return <ScrollView></ScrollView>;
}

export default LiveStreamDetailPage;
